# Script for gitlabs CI/CD Pipeline and Auto DevOps

# Run seperatly when installing on another device
#  - apt-get install -y sqlite3 libsqlite3-dev nodejs gradle default-jdk
#  - sudo curl -L --output /usr/local/bin/gitlab-runner https://gitlab-runner-downloads.s3.amazonaws.com/latest/binaries/gitlab-runner-linux-amd64
  
variables:
  GRADLE_OPTS: 
    -Dorg.gradle.daemon=false
    
#Anything in here will run before each stage
before_script:
  
  
# Run the following commands
build:
  stage: build
  script:
    - mkdir build
    - mkdir libs
    - cd libs
    - touch changelog.html
    - cd
    - gradle build
    - git remote set-url origin https://github.com/BuiltBrokenModding/SBM-BaggableMobs
    - chmod 770 gradlew
    - chmod a+x ./gradlew
    - ./gradlew clean build publish makeChangelog curseforge312123 -S -i --refresh-dependencies
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: push
    paths:
      - libs
      - build/libs
      - build
      - .gradle
  artifacts:
    paths:
      - libs/changelog.html
      - /build/libs/changelog.html
      - build/libs/*.jar
    expire_in: 1 week
      
    
deploy:
  stage: deploy
  script:
    - mkdir libs
    - cd libs
    - touch changelog.html
    - cd
    - git remote set-url origin https://github.com/BuiltBrokenModding/SBM-BaggableMobs
    - chmod 770 gradlew
    - ./gradlew clean build publish makeChangelog curseforge312123 -S -i --refresh-dependencies
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: pull
    paths:
      - build/libs
      - libs
      - build
      - .gradle
  artifacts:
    paths:
      - /changelog.html
    expire_in: 1 week

